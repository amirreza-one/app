<%= form_for(@likable) do |f| %>
  <% if @likable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@likable.errors.count, "error") %> prohibited this likable from being saved:</h2>

      <ul>
      <% @likable.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :photo_event_id %><br>
    <%= f.text_field :photo_event_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
